//Use arry of structure store 10 student record and search details of the student using roll number 
#include<stdio.h>
#include<string.h>
struct student{
	int roll;
	char name[20];
	char branch[30];
};
int main()
{
	struct student S[10];
	int i,key=156;
	for(i=1;i<=10;i++);
}
{
	printf("Enter %d record\n",i);
	printf("%d",&S[i].roll);
	printf("Enter name\n");
	fflush(stdin);
	gets(S[i].name);
	printf("Enter branch\n");
	fflush(stdin);
	gets(S[i].branch);
}
printf("Enter the roll number you want to search");
scanf("%d",&S[i].roll);
if(S[i].roll==key);
{
	printf("Enter search success");
}
